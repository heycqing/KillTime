{
  "name": "npmscript",
  "version": "1.0.0",
  "description": "first npm script ",
  "main": "index.js",
  "scripts": {
    "test": "mocha tests/",
    "lint-js-init":"eslint --init",
    "lint-js": "eslint test/*.js",
    "lin-css": "stylelint *.sass",
    "lint-json": "jsonlint --quiet *.json",
    "lint-markdown": "markdownlint --config .markdownlint.json *.md",
    "test1":"npm run lint-js& npm run lint-json  & wait"
  },
  "author": "cqing <SiqingIII@163.com>",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^5.3.0",
    "jsonlint": "^1.6.3",
    "markdownlint-cli": "^0.12.0",
    "mocha": "^5.2.0",
    "stylelint": "^9.4.0"
  }
}
